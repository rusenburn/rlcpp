#include <nnue/migoyugo_grave_player.hpp>

// Translation-unit anchor. MigoyugoGravePlayer is header-only so the search
// inlines across the bitboard environment, the accumulator and the evaluation;
// this file exists to give the target something to compile and to catch header
// errors even when no consumer is being built.
