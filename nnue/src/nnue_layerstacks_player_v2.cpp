#include <nnue/nnue_layerstacks_player_v2.hpp>

// Translation-unit anchor. NNUELayerStacksPlayerV2 is header-only so the
// search inlines across the accumulator, the evaluation and the bitboard
// environment; this file exists to give the target something to compile and
// to catch header errors even when no consumer is being built.
